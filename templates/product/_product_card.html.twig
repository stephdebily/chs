<div class="card">
	<img src="{{ p.Picture }}" width="200" length="200" class="img-fluid">
	<div class="card-body">
		<h4 class="card-title">{{ p.name }}</h4>
		<span class="badge bg-info">
			{{ p.category.name }}
		</span>
		<h4>{{ p.price }}
			&euro;</h4>

		<p class="card-text">{{ p.shortDescription }}</p>
		<a href="{{ path('product_show', {'category.slug': p.category.slug, 'slug' : p.slug }) }}" class="btn btn-primary btn-sm">Détails</a>
		<a href="{{ path('cart_add', {'id': p.id}) }}" class="btn btn-success btn-sm">Ajouter</a>
	</div>
</div>
